import { CustomMetrics } from "./SharedInterfaces";
export interface IOperationTimer {
    /** The (user?) operation that started this chain of events */
    originOperation: string;
    /** create an operation timer */
    createOperationTimer(operationName: string): () => void;
    /** gets the current duration stats from the recorded operations */
    getStats(): CustomMetrics;
}
/**
 * All of the event types supported AI/shell telemetry service.
 */
export declare const enum EventType {
    Event = 1,
    Trace = 2,
    Warning = 3,
    Error = 4
}
