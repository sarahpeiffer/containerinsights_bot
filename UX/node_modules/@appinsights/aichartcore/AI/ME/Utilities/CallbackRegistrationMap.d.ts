import * as Interfaces from "../SharedInterfaces";
export declare class CallbackRegistrationMap implements Interfaces.IRegistrationMap {
    private _map;
    Register(registrationInfo: Interfaces.IRegistrationInfo): void;
    Unregister(id: string): void;
    ExecuteAction(id: string, action: (element: Interfaces.IRegistrationInfo) => void): void;
    /**
     * Indicating whether the map is empty meaning that there's no subscriber register with it.
     */
    IsEmpty(): boolean;
    /**
     * For testing purpose
     */
    GetCallbackRegistrationInfo(id: string): Interfaces.IRegistrationInfo;
}
