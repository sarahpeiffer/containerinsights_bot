import { IPillContentConfig } from "./IPillContent";
import { OptionRendererHandler, ValueRendererHandler } from "@appinsights/react-select";
import { PillOption } from "./CommonInterfaces";
export declare type DropdownOption = PillOption;
export interface DropdownPillConfigData extends IPillContentConfig {
    /** Whether values are loading */
    areValuesLoading: boolean;
    /** values shown in Dropdown */
    valuesList: DropdownOption[];
    /** Allow multi-selection on the values Dropdown */
    enableValueMultiSelect?: boolean;
    /** Provide preselected values */
    selection?: DropdownOption[];
    /**
     * If true, labels are shown for each Dropdown. Otherwise, they are hidden.
     */
    showLabels?: boolean;
    /**
     * Whether dropdown should automatically open on pill expansion.
     */
    autoOpen?: boolean;
    /**
     * Custom renderer for dropdown options
     */
    optionRenderer?: OptionRendererHandler;
    /**
     * Custom renderer for the selected value
     */
    valueRenderer?: ValueRendererHandler;
}
export interface DropdownPillConfig extends DropdownPillConfigData {
    /** React to value selection changing on commit */
    onDropdownPillInputChange?: (newValue: string) => void;
    /** React to value selection changing. */
    onSelectionChange(newSelection: DropdownOption[]): void;
}
export interface DropdownPillProps extends DropdownPillConfig {
    editMode: boolean;
    updateConfiguredStatus: (configuration: boolean) => void;
    updateAriaLabel: (newLabel: string) => void;
    removeSelf: () => void;
}
