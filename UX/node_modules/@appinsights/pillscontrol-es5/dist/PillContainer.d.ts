import * as React from "react";
import { PillParameters } from "./Pill";
import "./Styles/PillContainer";
import "./Styles/Pill";
export interface PillContainerProps {
    /**
     * Unique id for this pill container.
     */
    containerId: string;
    /**
     * Defines the contents of the pills in this container.
     */
    pills: PillParameters[];
    /**
     * If set to Enabled, add button is shown and enabled
     * If set to Disabled, add button is shown and disabled
     * If set to Hidden, add button is hidden and disabled
     */
    addButtonStatus?: AddButtonStatus;
    /**
     * Always show the add label. Otherwise, it is only shown on empty containers.
     */
    alwaysShowLabel?: boolean;
    /**
     * Label to use for the add button for this container. If none is given, no label is shown.
     */
    addLabel?: string;
    /**
     * Icon to use for the add button for this container. If none is given, default + is used.
     */
    addIcon?: JSX.Element;
    /**
     * A custom class to apply on the container.
     */
    className?: string;
    /**
     * Whether pill should expand on creation.
    */
    autoExpand?: boolean;
    /**
     * Callback that tells the consumer when a user has clicked the remove button on a pill.
     * Note this is just a notification, in reaction to this callback the consumer must remove the pill from the pills array it passed in.
     */
    onRemove(containerId: string, pillId: string): void;
    /**
     * Called on edit mode changes to allow async loading
     */
    onEditModeChange?(containerId: string, pillId: string, inEditMode: boolean): void;
    /**
     * Callback that tells the consumer when a user has clicked the add button.
     * Note this is just a notification, in reaction to this callback the consumer must add a pill to the pills array it passed in.
     */
    onAdd(containerId: string): void;
    /**
     * Called on click an editing disabled pill.
     */
    onClick?(containerId: string, pillId: string): void;
}
/**
 * If set to Enabled (default value), add button is shown and enabled
 * If set to Disabled, add button is shown and disabled
 * If set to Hidden, add button is hidden and disabled
 */
export declare enum AddButtonStatus {
    Enabled = 0,
    Disabled = 1,
    Hidden = 2
}
export declare const PillContainer: React.SFC<PillContainerProps>;
