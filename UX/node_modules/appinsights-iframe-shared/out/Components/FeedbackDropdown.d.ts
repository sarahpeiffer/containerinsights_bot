/// <reference types="react" />
import * as React from 'react';
import { MenuBarItemProps } from './menubar';
import { PortalMessageService, TelemetryContext } from '../';
import { StringMap } from '../';
export interface FeedbackDropdownProps extends MenuBarItemProps {
    strings: StringMap<string>;
    message: string;
    messageService: PortalMessageService;
    telemetryContext: TelemetryContext;
    /** Boolean value indicating that the dropdown is not in a menu, so left/right arrows shouldn't close it */
    notPartOfMenu?: boolean;
}
export declare class FeedbackDropdown extends React.Component<FeedbackDropdownProps, {}> {
    static defaultProps: Partial<FeedbackDropdownProps>;
    private _onSelected;
    constructor(props: FeedbackDropdownProps);
    render(): JSX.Element;
    private onSelected(selected);
}
