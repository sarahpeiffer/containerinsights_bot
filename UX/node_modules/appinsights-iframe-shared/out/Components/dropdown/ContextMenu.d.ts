/// <reference types="react" />
import * as React from 'react';
import { DropdownLink, DropdownMessage, DropdownAction, DropdownDivider } from '../dropdown/DropdownTypes';
import { PortalMessageService } from '../../Services/PortalMessageService';
export interface ContextMenuProps {
    /**
     * The menu options for the flyout.
     */
    options: (DropdownDivider | DropdownLink | DropdownMessage | DropdownAction)[];
    /**
     * Portal Message Service instance to use to send messages to the extension.
     */
    messageService: PortalMessageService;
    /**
     * Optional value for the `tabindex` attribute on the button.
     */
    tabindex?: number;
}
export interface ContextMenuState {
    visible: boolean;
}
export declare class ContextMenu extends React.Component<ContextMenuProps, ContextMenuState> {
    private _button;
    private _container;
    private _flyout;
    private _onClick;
    private _onClose;
    private _onKeyDown;
    private _onWindowClick;
    constructor(props: ContextMenuProps);
    focus(): void;
    onClick(): void;
    componentWillMount(): void;
    componentDidMount(): void;
    componentWillUpdate(nextProps: ContextMenuProps, nextState: ContextMenuState): void;
    componentDidUpdate(prevProps: ContextMenuProps, prevState: ContextMenuState): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
    private adjust();
    private close();
    private onKeyDown(e);
    private onWindowClick(e);
}
