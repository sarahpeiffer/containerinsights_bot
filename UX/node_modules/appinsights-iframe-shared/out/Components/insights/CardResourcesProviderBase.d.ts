/// <reference types="react" />
import { StringMap } from '../../';
import { InsightDataObjBase } from './InsightDataObjBase';
export interface CardHeader {
    title: JSX.Element;
    subtitle: string;
    icon: JSX.Element;
    isLoading: boolean;
    ariaLabel: string;
}
/**
 * Abstract resources provider base for an insight card.
 * Each card type should have its own resource provider that inherits from this base.
 */
export declare abstract class CardResourcesProviderBase {
    strings: StringMap<string>;
    protected detection: InsightDataObjBase;
    protected constructor(strings: StringMap<string>, detection: InsightDataObjBase);
    abstract getCardHeader(): CardHeader;
    abstract getCardContent(): JSX.Element;
}
