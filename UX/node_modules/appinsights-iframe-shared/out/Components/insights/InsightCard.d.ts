/// <reference types="react" />
import * as React from 'react';
import { CardResourcesProviderBase } from './CardResourcesProviderBase';
export interface InsightCardProps {
    resourcesProvider: CardResourcesProviderBase;
    onClick?: () => void;
    tooltip?: string;
}
export interface InsightCardState {
}
export declare class InsightCard extends React.Component<InsightCardProps, InsightCardState> {
    constructor(props: InsightCardProps);
    render(): JSX.Element;
    private onKeyDown(e);
    private onClick(e);
}
