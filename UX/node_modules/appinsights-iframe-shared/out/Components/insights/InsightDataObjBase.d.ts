/** Abstract base for each insight data object. */
export declare abstract class InsightDataObjBase {
    /** The insight unique id */
    id: string;
    /** The high level type of the insight (e.g. auto-cluster, smart-detection etc) */
    insightType: string;
    constructor(id: string, insightType: string);
}
