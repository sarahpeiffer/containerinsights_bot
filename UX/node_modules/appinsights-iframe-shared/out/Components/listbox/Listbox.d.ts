/// <reference types="react" />
import * as React from 'react';
export interface ListboxOption {
    value: any;
    label: string;
    id?: string;
}
export interface ListboxProps {
    id: string;
    isFocused?: boolean;
    tabIndex?: number;
    options: ListboxOption[];
    selectedIndex?: number;
    label: string;
    onChange: (value: ListboxOption) => void;
}
export interface ListboxState {
    isOpen: boolean;
    selectedIndex: number;
}
export declare class Listbox extends React.Component<ListboxProps, ListboxState> {
    private _dom;
    private _bind;
    private _shouldFocus;
    constructor(props: ListboxProps);
    componentWillReceiveProps(nextProps: ListboxProps): void;
    render(): JSX.Element;
    componentDidMount(): void;
    componentDidUpdate(prevProps: ListboxProps, prevState: ListboxState): void;
    private focus();
    private bind(e);
    private getOptions();
    private onOptionClick(index);
}
