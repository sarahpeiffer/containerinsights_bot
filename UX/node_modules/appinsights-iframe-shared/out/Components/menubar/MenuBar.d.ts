/// <reference types="react" />
import * as React from 'react';
export interface MenuBarProps {
    elements: MenuBarItem[];
    label: string;
    className: string;
}
export interface MenuBarItem {
    id: string;
    className: string;
    label: string;
    element: (props: MenuBarItemProps) => JSX.Element;
}
export interface MenuBarItemProps {
    isFocused?: boolean;
    tabIndex?: number;
    role?: string;
}
export interface MenuBarState {
    focusIndex: number;
}
/**
 * An accessible horizontal menu bar that supports arrow key navigation.
 */
export declare class MenuBar extends React.Component<MenuBarProps, MenuBarState> {
    private _onKeyDown;
    private _ul;
    private _menu;
    private _ref;
    constructor(props: MenuBarProps);
    componentWillUpdate(props: MenuBarProps): void;
    render(): JSX.Element;
    private getItems();
    private onKeyDown(e);
}
