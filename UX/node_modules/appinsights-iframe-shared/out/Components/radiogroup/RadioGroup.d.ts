/// <reference types="react" />
import * as React from 'react';
export interface RadioGroupProps {
    items: RadioItem[];
    className?: string;
    label: string;
    onChange: (item: RadioItem) => void;
    selectedIndex?: number;
}
export interface RadioItem {
    content: JSX.Element;
    className?: string;
    id: string;
    value: any;
    contentId: string;
    title?: string;
}
export interface RadioGroupState {
    selectedIndex: number;
    focusedIndex: number;
}
/**
 * A component that provides an accessible list of radio buttons, supporting all necessary keyboard actions.
 */
export declare class RadioGroup extends React.Component<RadioGroupProps, RadioGroupState> {
    private _onKeyDown;
    private _bind;
    private _dom;
    constructor(props: RadioGroupProps);
    render(): JSX.Element;
    componentWillReceiveProps(nextProps: RadioGroupProps): void;
    componentDidUpdate(prevProps: RadioGroupProps, prevState: RadioGroupState): void;
    private onItemKeyDown(item, e, i);
    private setFocus(i);
    private bind(e);
    private onKeyDown(e);
    private onClick(item, i);
}
