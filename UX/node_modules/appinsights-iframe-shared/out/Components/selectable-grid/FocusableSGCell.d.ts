/// <reference types="react" />
import * as React from 'react';
export interface FocusableSGCellProps {
    children: React.ReactNode;
    items: number;
    onClick: (index: number) => void;
    onFocus: (index: number) => void;
}
export declare class FocusableSGCell extends React.Component<FocusableSGCellProps> {
    private _container;
    private _set;
    private _onClick;
    private _onKeyDown;
    constructor(props: FocusableSGCellProps);
    componentDidUpdate(): void;
    render(): JSX.Element;
    private onClick();
    private onKeyDown(e);
}
