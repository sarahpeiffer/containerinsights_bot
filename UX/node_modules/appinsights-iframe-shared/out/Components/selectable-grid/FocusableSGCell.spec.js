import * as React from 'react';
import { FocusableSGCell } from '.';
describe('FocusableSGCell', function () {
    describe('render', function () {
        it('renders children in a div', function () {
            var props = {
                children: React.createElement("b", null),
                items: 0,
                onClick: function () { },
                onFocus: function () { }
            };
            var cell = new FocusableSGCell(props);
            var rendered = cell.render();
            expect(rendered.type).toBe('div');
            expect(rendered.props.children.type).toBe('b');
        });
    });
});
//# sourceMappingURL=FocusableSGCell.spec.js.map