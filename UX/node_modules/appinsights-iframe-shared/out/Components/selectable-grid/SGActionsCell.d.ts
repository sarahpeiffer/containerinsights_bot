/// <reference types="react" />
import * as React from 'react';
import { SGCellProps } from './SelectableGridData';
import { DropdownLink, DropdownMessage, DropdownAction, DropdownDivider } from '../dropdown/DropdownTypes';
import { PortalMessageService } from '../../Services/PortalMessageService';
export interface SGActionsCellValue {
    /**
     * Actions to display in the cell.
     */
    actions: DropdownMessage[];
    /**
     * The menu options for the flyout.
     */
    menuOptions: (DropdownDivider | DropdownLink | DropdownMessage | DropdownAction)[];
}
export declare type SGActionsCellProps = SGCellProps<SGActionsCellValue>;
export interface SGActionsCellClassProps extends SGActionsCellValue {
    messageService: PortalMessageService;
}
export declare class SGActionsCellClass extends React.Component<SGActionsCellClassProps> {
    private _onClick;
    private _onFocus;
    private _actions;
    private _contextMenu;
    render(): JSX.Element;
    private onActionClick(index);
    private onClick(index);
    private onFocus(index);
}
/**
 * Cell for use in `SelectableGrid` to display actions for a row, both in the cell and in a context menu.
 *
 * @param messageService Portal Message Service instance to use to send messages to the extension.
 */
export declare const SGActionsCell: (messageService: PortalMessageService) => React.StatelessComponent<SGCellProps<SGActionsCellValue>>;
