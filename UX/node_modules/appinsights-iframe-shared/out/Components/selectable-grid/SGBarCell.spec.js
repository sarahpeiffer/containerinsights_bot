import { SGBarCell } from './SGBarCell';
describe('SGBarCell', function () {
    it('should create a div with the supplied bar width', function () {
        var cell = SGBarCell('a')({ value: { a: 0.9 }, rowSelected: false });
        expect(cell.type).toBe('div');
        expect(cell.props.className).toBe('sg-barcell');
        expect(cell.props.children[0].props.className).toBe('sg-barbox');
        expect(cell.props.children[0].props.style.width).toBe('90%');
        expect(cell.props.children[1].props.className).toBe('sg-barbox-sub');
        expect(cell.props.children[1].props.style.width).toBe('0%');
    });
    it('should create a div with the supplied sub-bar width', function () {
        var cell = SGBarCell('a', 'b')({ value: { a: 0.9, b: 0.5 }, rowSelected: false });
        expect(cell.type).toBe('div');
        expect(cell.props.className).toBe('sg-barcell');
        expect(cell.props.children[0].props.className).toBe('sg-barbox');
        expect(cell.props.children[0].props.style.width).toBe('90%');
        expect(cell.props.children[1].props.className).toBe('sg-barbox-sub');
        expect(cell.props.children[1].props.style.width).toBe('50%');
    });
});
//# sourceMappingURL=SGBarCell.spec.js.map