export function TooltipDetector(value) {
    return function (elm) {
        if (!elm) {
            return;
        }
        if (elm.clientWidth < elm.scrollWidth || elm.clientHeight < elm.scrollHeight) {
            elm.title = value;
        }
        else {
            elm.title = '';
        }
    };
}
//# sourceMappingURL=SGHelper.js.map