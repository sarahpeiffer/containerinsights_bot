import { SGPlainCell, SGFormattedPlainCell } from './SGPlainCell';
describe('SGPlainCell', function () {
    it('should create a div with the supplied value', function () {
        var cell = SGPlainCell({ value: 'test', rowSelected: false });
        expect(cell.type).toBe('div');
        expect(cell.props.className).toBe('sg-text');
        expect(cell.props.children).toBe('test');
    });
});
describe('SGFormattedPlainCell', function () {
    it('should create a div with the supplied value modified by the formatter', function () {
        var cell = SGFormattedPlainCell(function (a) { return a + 'formatted'; })({ value: 'test', rowSelected: false });
        expect(cell.type).toBe('div');
        expect(cell.props.className).toBe('sg-text');
        expect(cell.props.children).toBe('testformatted');
    });
});
//# sourceMappingURL=SGPlainCell.spec.js.map