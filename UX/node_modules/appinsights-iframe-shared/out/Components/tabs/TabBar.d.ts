/// <reference types="react" />
import * as React from 'react';
export interface TabItem {
    id: string;
    content: JSX.Element;
    value: any;
    /**
     * HTML ID of the element representing a tab panel.
     */
    htmlId: string;
    className: string;
}
export interface TabBarProps {
    tabs: TabItem[];
    onChange: (tab: TabItem) => void;
    className?: string;
    selectedIndex?: number;
}
export interface TabBarState {
    selectedIndex: number;
}
/**
 * A component for displaying an accessible list of tabs that supports all required keyboard attributes.
 * Content controlled by the tabs needs to have role='tabpanel' and needs to provide its HTML ID
 * to the relevant tab. Tab content should also have an appropriate aria-label.
 */
export declare class TabBar extends React.Component<TabBarProps, TabBarState> {
    private _onKeyDown;
    private _bind;
    private _dom;
    private _focusIndex;
    constructor(props: TabBarProps);
    componentWillReceiveProps(nextProps: TabBarProps): void;
    render(): JSX.Element;
    private onKeyDown(e);
    private focus(i);
    private onTabKeyDown(e, tab, i);
    private onClick(tab, i);
    private bind(e);
}
