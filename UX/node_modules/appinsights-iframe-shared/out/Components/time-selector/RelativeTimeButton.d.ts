/// <reference types="react" />
import * as React from 'react';
import { TimeContext } from '../../TimeContext';
export interface RelativeTimeButtonProps {
    isSelected: boolean;
    onSelected: (timeContext: TimeContext) => void;
    value: string;
    durationMs: number;
    shouldFocus: boolean;
}
export declare const RelativeTimeButton: React.StatelessComponent<RelativeTimeButtonProps>;
