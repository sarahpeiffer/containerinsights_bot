import * as tslib_1 from "tslib";
import * as React from 'react';
import { TimeContext } from '../../TimeContext';
import { AccessibilityUtils } from '../../AccessibilityUtils';
export var RelativeTimeButton = function (_a) {
    var isSelected = _a.isSelected, onSelected = _a.onSelected, value = _a.value, durationMs = _a.durationMs, shouldFocus = _a.shouldFocus;
    var onSelect = function () {
        onSelected(new TimeContext(durationMs));
    };
    var focus = function (x) {
        if (x && shouldFocus) {
            x.focus();
        }
    };
    return React.createElement("li", tslib_1.__assign({}, AccessibilityUtils.getAccessibilityProps({
        className: isSelected ? 'selected' : '',
        onClick: onSelect,
        ref: focus,
        role: 'menuitem',
        tabIndex: shouldFocus ? 0 : -1
    }), { "aria-selected": isSelected }),
        React.createElement("div", { className: 'panel-list-button' }, value));
};
//# sourceMappingURL=RelativeTimeButton.js.map