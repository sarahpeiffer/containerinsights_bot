export function sanitizeKQLFilterValue(filterValue) {
    if (filterValue && !!filterValue.trim()) {
        var replacements = [
            ['\\', '\\\\'],
            ['"', '\\\"'],
            ['\'', '\\\''],
            ['\n', '\\n'],
            ['\r', '\\r'],
            ['\t', '\\t'],
        ];
        replacements.forEach(function (replacement) {
            filterValue = filterValue.split(replacement[0]).join(replacement[1]);
        });
    }
    return filterValue;
}
//# sourceMappingURL=KQLUtils.js.map