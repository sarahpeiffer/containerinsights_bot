import { LocalizationService } from './LocalizationService';
import { IPortalMessageService, IPortalMessageSubscriber, ITelemetryService } from '../SharedInterfaces';
export declare class CommunicationChannel {
    private _portalMessageService;
    private _localizationService;
    private _telemetryService;
    constructor(messageService: IPortalMessageService, localizationService: LocalizationService, telemetryService: ITelemetryService);
    /**
     * Set up communication between iframe and view model on the Portal side.
     * This method is designed to work with ReactIFrameViewModel class.
     */
    Initialize(setInstanceId?: (id: string) => any, sendReady?: boolean): IPortalMessageSubscriber[];
    sendReady(): void;
}
