export declare class FontManager {
    private static GetFontLocale(locale);
    /**
     * Inject localized font styles from Ibiza into the iframe styles
     */
    static InjectFonts(locale: string): void;
}
