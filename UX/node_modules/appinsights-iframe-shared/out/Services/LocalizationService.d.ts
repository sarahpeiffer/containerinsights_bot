import { StringMap } from '../SharedInterfaces';
import { IPortalMessageService, IPortalMessageSubscriber } from '../SharedInterfaces';
export declare class LocalizationService {
    private strings;
    private _initPromise;
    private _initCallback;
    private _initRejection;
    private _fallback;
    constructor(fallback?: StringMap<StringMap<string> | string>);
    Register(messageSvc: IPortalMessageService, messageName: string): IPortalMessageSubscriber;
    GetStrings(): Promise<StringMap<string>>;
    private UpdateStrings(message);
}
