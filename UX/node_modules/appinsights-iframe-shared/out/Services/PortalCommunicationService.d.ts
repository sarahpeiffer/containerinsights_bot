import { PortalMessageService } from '..';
export interface PortalCommunicationPayload {
    /** Unique identifier for matching a request to a response */
    id: string;
}
/**
 * Service for communicating with the view model with a request/response model
 */
export declare class PortalCommunicationService {
    private _messageService;
    constructor(messageService: PortalMessageService);
    /**
     * Send a message to the view model and wait for a response with the same message name and matching ID in the payload.
     * @param message
     * @param data
     */
    request<T extends PortalCommunicationPayload>(message: string, data?: any, responseIsSentSafely?: boolean): Promise<T>;
}
