import { ComponentId } from './DataTypes';
export declare class ComponentIdCache {
    private static _instance;
    private _cache;
    static Instance(): ComponentIdCache;
    Get(appId: string): ComponentId;
    Add(appId: string, componentId: ComponentId): void;
    Clear(): void;
}
