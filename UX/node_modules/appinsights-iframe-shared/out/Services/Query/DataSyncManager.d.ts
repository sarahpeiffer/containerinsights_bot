import { ITelemetryService } from '../../SharedInterfaces';
export declare class DataSyncManager {
    private _telemetryService;
    private _sync;
    constructor(telemetryService: ITelemetryService);
    GetSyncIdForQuery(queryId: string): number;
    IsQueryStillValid(queryId: string, syncId: number): boolean;
    IssueQuery<T>(queryId: string, promise: Promise<T>, success: (value: T, syncId?: number) => void, error: (e: any, syncId?: number) => void, preQuery?: (syncId: number) => void): Promise<void>;
}
