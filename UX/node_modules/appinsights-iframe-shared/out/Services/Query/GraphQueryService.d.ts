import { ITelemetryService } from '../../SharedInterfaces';
import { QueryTelemetryContext } from './QueryTelemetryService';
import { IRouteConfiguration } from './IRouteConfiguration';
import { TelemetryContext } from '../TelemetryContext';
import { ComponentId } from './DataTypes';
/**
 * Executes Graph queries and returns response.
 */
export declare class GraphQueryService {
    private _ajaxHelper;
    private _cache;
    constructor(routeConfig: IRouteConfiguration, telemetryService: ITelemetryService, telemetryContext: TelemetryContext, instanceId: () => string);
    ExecuteQuery<TResult>(componentId: ComponentId, path: string, telemetryContext: QueryTelemetryContext, queryParams?: any): Promise<TResult>;
    private _executeAjax<TResult>(buildUri, telemetryContext);
}
