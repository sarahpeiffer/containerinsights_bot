import { AsyncSetting } from '../PortalConfigService';
export interface IRouteConfiguration {
    Token(): Promise<AsyncSetting<string>>;
    RouteName(): string;
    RouteInfo(): Promise<AsyncSetting<{
        Hosts: string[];
        RetryCount: number;
    }>>;
}
export interface IDraftConfiguration extends IRouteConfiguration {
    AppId(): Promise<AsyncSetting<string>>;
    Token(): Promise<AsyncSetting<string>>;
    RouteName(): string;
    RouteInfo(): Promise<AsyncSetting<{
        Hosts: string[];
        RetryCount: number;
    }>>;
}
