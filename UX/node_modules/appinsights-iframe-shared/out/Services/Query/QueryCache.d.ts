export declare class QueryCache<TParams, TResult> {
    private _cache;
    private _lifetime;
    private _keyFn;
    constructor(lifetime: number, keyFn: (query: TParams) => string);
    Get(query: TParams): TResult;
    Store(query: TParams, result: TResult): void;
    Clear(): void;
}
