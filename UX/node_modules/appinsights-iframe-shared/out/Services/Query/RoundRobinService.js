var RoundRobinService = /** @class */ (function () {
    function RoundRobinService() {
    }
    RoundRobinService.GetHostName = function (serviceId, uris) {
        if (!uris || !uris.length) {
            throw 'uris cannot be null or empty.';
        }
        var index = RoundRobinService._indices[serviceId];
        if (typeof index === 'undefined') {
            index = 0;
        }
        var uri = uris[index];
        ++index;
        if (index > uris.length - 1) {
            index = 0;
        }
        RoundRobinService._indices[serviceId] = index;
        return uri;
    };
    RoundRobinService._indices = {};
    return RoundRobinService;
}());
export { RoundRobinService };
//# sourceMappingURL=RoundRobinService.js.map