import { TimeSeries } from '../../SharedInterfaces';
export declare class TimeSeriesImpl {
    private _chunks;
    private _intervalInMs;
    private static isChunkAffected(start, end, startChunk, endChunk);
    constructor(intervalInMs: number);
    Get(start: Date, end: Date): Array<[Date, number]>;
    Store(start: Date, end: Date, data: TimeSeries): void;
    private exciseAffectedChunks(start, end);
}
export declare class TimeSeriesCache {
    private _cache;
    Get(key: string, start: Date, end: Date): TimeSeries;
    Store(key: string, intervalInMs: number, start: Date, end: Date, data: TimeSeries): void;
    Clear(): void;
}
