export declare const TelemetryConstants: {
    PartLoad: string;
    AjaxQuery: string;
    HotspotClicked: string;
};
export declare enum EventCategory {
    /** represents an event that wasn't a DIRECT action of a user.  it was not a click or press or type */
    Infrastructure = 0,
    /** represents an event that is a DIRECT ACTION of a user. the user pressed something, typed something, etc. */
    UserInteraction = 1,
}
