/**
 * list of properties that can change on the health pane view
 */
export class HealthPaneObservableProp {
    /** data loaded indicator */
    static DataLoaded = 'HealthPane.Loaded';

    /** selected health aspect identifier */
    static SelectedAspectIdentifier  = 'HealthPane.SelectedAspectIdentifier';

    /** selected monitor identifier */
    static SelectedMonitorIdentifier = 'HealthPane.SelectedMonitorIdentifier';
}
